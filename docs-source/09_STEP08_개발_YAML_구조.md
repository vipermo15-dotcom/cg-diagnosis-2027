# STEP 08. YAML 구조
diagnosis.yml / questions.yml / scoring.yml / tracks.yml / life-stages.yml / course-routing.yml / ai-profile.yml / results.yml / comparison.yml / roadmap.yml.
YAML을 콘텐츠와 routing의 기준으로 사용하고 질문 ID를 코드에 하드코딩하지 않는다.
