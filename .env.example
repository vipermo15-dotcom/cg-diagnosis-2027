# Supabase 연동 (선택 사항 — 비워두면 앱은 로컬 상태만으로 완전히 동작합니다)
# 1. Supabase 프로젝트를 생성한다
# 2. supabase/schema.md 의 SQL을 SQL Editor에서 실행한다
# 3. 아래 두 값을 프로젝트 설정 > API 에서 복사해 채운다
# 4. RLS 정책은 schema.md에 정의되어 있지 않으므로, 기관의 개인정보 처리 방침에 맞춰 별도로 설계해야 한다

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
